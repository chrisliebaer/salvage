{
	"username": "Salvage Daemon (${host})",
	"embeds": [
		{
			"title": "Tide `${tide}` completed!",
			"description": "The following volumes have been saved: ${volumes}",
			"color": 1561887,
			"fields": [
				{
					"name": "Duration",
					"value": "${duration}",
					"inline": true
				}
			],
			"footer": {
				"text": "${host}"
			}
		}
	]
}
